# Deploy SIVALID

Jalankan script deploy:

```bash
./deploy-sivalid.sh
```

Jika belum bisa dieksekusi:

```bash
chmod +x deploy-sivalid.sh
./deploy-sivalid.sh
```

Setelah selesai, buka aplikasi di browser.
